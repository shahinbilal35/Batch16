public class practice {
    public void xyz(integer n){
        integer power = n;
        for (Integer i = 1; i < n; i++) {
            power *= n;  
            system.debug(power);
        }
        system.debug(power);
    }
}